DS_Casino_System_Poker = {
    Target = 'qb-target', --false or 'qtarget' / 'ox_target' / 'qb-target'
    TimeLeftAfter = 10, -- time remaining after one player betted (dealer actions start timeout)
    PlayerDecideTime = 10, -- player decide time (when watching our cards)
    ShowCardsAfterReveal = true, -- showing cameras on the players cards when revealing them
    MinimumBet = 50,
    MaxBet = 50000
}